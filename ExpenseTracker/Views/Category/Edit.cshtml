@model ExpenseTracker.Models.CategoryModel




<section>
    <form asp-action="Edit" asp-controller="Category" method="post">
        <div asp-validation-summary="ModelOnly" class="text-danger"></div>
        <input type="hidden" asp-for="CategoryId"/>
        <div class="row mt-2 justify-content-center">
            <div class="card card-information">
                <div class="card-header">
                    <h2> Update Category @Model.Title</h2>
                </div>
                <div class="card-body">
                    @*Row1*@
                    <div class="row">
                        @*Title*@
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="Title" class="col-form-label small font-w-700 dark-color m-0px"></label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fa-solid fa-layer-group"></i></span>
                                    <input type="text" class="form-control text-box single-line" asp-for="Title" placeholder="@Model.Title" />
                                </div>
                                <span asp-validation-for="Title" class="text-danger"></span>
                            </div>
                        </div>
                        @*Icon*@
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="Icon" id="emoji-instructions" class="col-form-label small font-w-700 dark-color m-0px"></label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fa-solid fa-icons"></i></span>
                                    <input type="text" class="form-control text-box single-line" asp-for="Icon" placeholder="@Model.Icon" />
                                </div>
                                <span asp-validation-for="Icon" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                    @*Row2*@
                    <div class="row">
                        @*Type*@
                        <div class="col-md-4">
                            <div class="form-group">
                                <label asp-for="Type" class="col-form-label small font-w-700 dark-color m-0px"></label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fa-solid fa-plus-minus"></i></span>
                                    <select class="form-control text-box single-line" asp-for="Type" placeholder="@Model.Type">
                                        <option value=""></option>
                                        <option value="Expense">Expense</option>
                                        <option value="Income">Income</option>
                                    </select>
                                </div>
                                <span asp-validation-for="Type" class="text-danger"></span>
                            </div>
                        </div>
                        @*CreationDate*@
                        <div class="col-md-4">
                            <div class="form-group">
                                <label asp-for="CreationDate" class="col-form-label small font-w-700 dark-color m-0px"></label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fa-solid fa-calendar"></i></span>
                                    <input type="date" class="form-control text-box single-line" asp-for="CreationDate" placeholder="@Model.CreationDate" />
                                </div>
                                <span asp-validation-for="CreationDate" class="text-danger"></span>
                            </div>
                        </div>
                        @*Note*@
                        <div class="col-md-4">
                            <div class="form-group">
                                <label asp-for="Note" class="col-form-label small font-w-700 dark-color m-0px"></label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fa-solid fa-plus-minus"></i></span>
                                    <input type="text" class="form-control text-box single-line" asp-for="Note" placeholder="@Model.Note" />
                                </div>
                            </div>
                        </div>
                    </div>
                    @*Row3*@
                    <div class="row">
                        @*Priority*@
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="Priority" class="col-form-label small font-w-700 dark-color m-0px"></label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fa-solid fa-exclamation"></i></span>
                                    <select class="form-control text-box single-line" asp-for="Priority" placeholder="@Model.Priority">
                                        <option value=""></option>
                                        <option value="High">High</option>
                                        <option value="Medium">Medium</option>
                                        <option value="Low">Low</option>
                                    </select>
                                </div>
                                <span asp-validation-for="Priority" class="text-danger"></span>
                            </div>
                        </div>
                        @*Recurring*@
                        <div class="col-md-6">
                            <div class="form-group">
                                <label asp-for="Recurring" class="col-form-label small font-w-700 dark-color m-0px"></label>
                                <div class="input-group">
                                    <span class="input-group-text"><i class="fa-solid fa-repeat"></i></span>
                                    <select class="form-control text-box single-line" asp-for="Recurring" placeholder="@Model.Priority">
                                        <option value="1">Yes</option>
                                        <option value="0">No</option>
                                    </select>
                                </div>
                                <span asp-validation-for="Recurring" class="text-danger"></span>
                            </div>
                        </div>
                    </div>
                    @*Create Button*@
                    <div class="d-flex">
                        <button class="btn btn-primary btn-sm w-auto">Update your Category</button>
                    </div>
                </div>
            </div>
        </div>
    </form>
</section>


<script>
    // Function to detect the operating system
    function detectOS() {
        var userAgent = window.navigator.userAgent;
        var platform = window.navigator?.userAgentData?.platform || window.navigator.platform;
        var macosPlatforms = ['Macintosh', 'MacIntel', 'MacPPC', 'Mac68K'];
        var windowsPlatforms = ['Win32', 'Win64', 'Windows', 'WinCE'];
        var iosPlatforms = ['iPhone', 'iPad', 'iPod'];
        var os = null;

        if (macosPlatforms.indexOf(platform) !== -1) {
            os = 'MacOS';
        } else if (iosPlatforms.indexOf(platform) !== -1) {
            os = 'iOS';
        } else if (windowsPlatforms.indexOf(platform) !== -1) {
            os = 'Windows';
        } else if (/Android/.test(userAgent)) {
            os = 'Android';
        } else if (!os && /Linux/.test(platform)) {
            os = 'Linux';
        }

        return os;
    }

    // Function to update the label text based on the detected OS
    function updateEmojiInstructions() {
        var os = detectOS();
        var instructionsLabel = document.getElementById('emoji-instructions');

        switch (os) {
            case 'Windows':
                instructionsLabel.textContent = 'Add your Icon (Press Win + . to access emojis)';
                break;
            case 'MacOS':
                instructionsLabel.textContent = 'Add your Icon (Press Control + Command + Space to access emojis)';
                break;
            case 'Linux':
                instructionsLabel.textContent = 'Add your Icon (Refer to your Linux distribution documentation to access emojis)';
                break;
            default:
                instructionsLabel.textContent = 'Add your Icon';
        }
    }
    // Call the function on page load
    window.onload = updateEmojiInstructions;
</script>
@section Scripts {
    @{await Html.RenderPartialAsync("_ValidationScriptsPartial");}

}
